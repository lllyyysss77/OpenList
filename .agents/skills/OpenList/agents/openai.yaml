interface:
  display_name: "OpenList"
  short_description: "Repo-specific patterns and workflows for OpenList"
  default_prompt: "Use the OpenList repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true